<Application x:Class="SistemPerpustakaan.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:pembantu="clr-namespace:SistemPerpustakaan.Pembantu"
             ShutdownMode="OnLastWindowClose">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Warna -->
            <SolidColorBrush x:Key="WarnaUtama"        Color="#2563EB"/>
            <SolidColorBrush x:Key="WarnaUtamaGelap"   Color="#1D4ED8"/>
            <SolidColorBrush x:Key="WarnaAdmin"         Color="#7C3AED"/>
            <SolidColorBrush x:Key="WarnaPengguna"      Color="#0891B2"/>
            <SolidColorBrush x:Key="WarnaBahaya"        Color="#DC2626"/>
            <SolidColorBrush x:Key="WarnaSukses"        Color="#16A34A"/>
            <SolidColorBrush x:Key="WarnaPeringatan"    Color="#D97706"/>
            <SolidColorBrush x:Key="WarnaLatarAbu"      Color="#F8FAFC"/>
            <SolidColorBrush x:Key="WarnaTeksPrimer"    Color="#1E293B"/>
            <SolidColorBrush x:Key="WarnaTeksSecunder"  Color="#64748B"/>
            <SolidColorBrush x:Key="WarnaBorder"        Color="#E2E8F0"/>

            <!-- Tombol Utama -->
            <Style x:Key="TombolUtama" TargetType="Button">
                <Setter Property="Background"      Value="{StaticResource WarnaUtama}"/>
                <Setter Property="Foreground"      Value="White"/>
                <Setter Property="FontSize"        Value="14"/>
                <Setter Property="FontWeight"      Value="SemiBold"/>
                <Setter Property="Padding"         Value="20,10"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor"          Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="8" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource WarnaUtamaGelap}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#94A3B8"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Tombol Bahaya -->
            <Style x:Key="TombolBahaya" TargetType="Button" BasedOn="{StaticResource TombolUtama}">
                <Setter Property="Background" Value="{StaticResource WarnaBahaya}"/>
            </Style>

            <!-- Tombol Sukses -->
            <Style x:Key="TombolSukses" TargetType="Button" BasedOn="{StaticResource TombolUtama}">
                <Setter Property="Background" Value="{StaticResource WarnaSukses}"/>
            </Style>

            <!-- Tombol Sekunder (outline) -->
            <Style x:Key="TombolSekunder" TargetType="Button">
                <Setter Property="Background"      Value="Transparent"/>
                <Setter Property="Foreground"      Value="{StaticResource WarnaUtama}"/>
                <Setter Property="FontSize"        Value="14"/>
                <Setter Property="Padding"         Value="20,10"/>
                <Setter Property="BorderBrush"     Value="{StaticResource WarnaUtama}"/>
                <Setter Property="BorderThickness" Value="1.5"/>
                <Setter Property="Cursor"          Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#EFF6FF"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- TextBox -->
            <Style x:Key="KotakTeks" TargetType="TextBox">
                <Setter Property="FontSize"        Value="14"/>
                <Setter Property="Padding"         Value="12,10"/>
                <Setter Property="BorderBrush"     Value="{StaticResource WarnaBorder}"/>
                <Setter Property="BorderThickness" Value="1.5"/>
                <Setter Property="Background"      Value="White"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8">
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Padding="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="BorderBrush" Value="{StaticResource WarnaUtama}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- PasswordBox -->
            <Style x:Key="KotakSandi" TargetType="PasswordBox">
                <Setter Property="FontSize"        Value="14"/>
                <Setter Property="Padding"         Value="12,10"/>
                <Setter Property="BorderBrush"     Value="{StaticResource WarnaBorder}"/>
                <Setter Property="BorderThickness" Value="1.5"/>
                <Setter Property="Background"      Value="White"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="PasswordBox">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8">
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Padding="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="BorderBrush" Value="{StaticResource WarnaUtama}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- DataGrid -->
            <Style x:Key="TabelData" TargetType="DataGrid">
                <Setter Property="Background"               Value="White"/>
                <Setter Property="BorderThickness"          Value="1"/>
                <Setter Property="BorderBrush"              Value="{StaticResource WarnaBorder}"/>
                <Setter Property="GridLinesVisibility"      Value="Horizontal"/>
                <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource WarnaBorder}"/>
                <Setter Property="RowHeight"                Value="48"/>
                <Setter Property="FontSize"                 Value="13"/>
                <Setter Property="AutoGenerateColumns"      Value="False"/>
                <Setter Property="CanUserAddRows"           Value="False"/>
                <Setter Property="CanUserDeleteRows"        Value="False"/>
                <Setter Property="SelectionMode"            Value="Single"/>
                <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
            </Style>

            <!-- DataGrid row hover + cursor -->
            <Style TargetType="DataGridRow">
                <Setter Property="Cursor" Value="Hand"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#F0F7FF"/>
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="#DBEAFE"/>
                        <Setter Property="BorderBrush" Value="#93C5FD"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- DataGridCell — hilangkan highlight bawaan -->
            <Style TargetType="DataGridCell">
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- ScrollBar tipis modern -->
            <Style TargetType="ScrollBar">
                <Setter Property="Width" Value="6"/>
                <Setter Property="MinWidth" Value="6"/>
            </Style>

            <!-- Konverter -->
            <pembantu:BoolToWarnaConverter     x:Key="BoolToWarnaConverter"/>
            <pembantu:BoolToWarnaTeksConverter  x:Key="BoolToWarnaTeksConverter"/>
            <pembantu:BoolInverterConverter    x:Key="BoolInverterConverter"/>
            <pembantu:StringToInisialConverter x:Key="StringToInisialConverter"/>

        </ResourceDictionary>
    </Application.Resources>
</Application>
