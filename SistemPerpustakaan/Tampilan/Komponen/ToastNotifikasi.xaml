<UserControl x:Class="SistemPerpustakaan.Tampilan.Komponen.ToastNotifikasi"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             HorizontalAlignment="Right" VerticalAlignment="Bottom"
             Margin="0,0,24,24" Panel.ZIndex="999"
             Visibility="Collapsed" IsHitTestVisible="False">

    <UserControl.RenderTransform>
        <TranslateTransform x:Name="SlideTransform" Y="80"/>
    </UserControl.RenderTransform>

    <Border x:Name="ToastBorder" CornerRadius="12"
            MinWidth="280" MaxWidth="380"
            Padding="16,13">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="0" Opacity="0.2" Color="#000"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Ikon status -->
            <Border Grid.Column="0" Width="32" Height="32" CornerRadius="16"
                    Margin="0,0,12,0" VerticalAlignment="Center">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#FFFFFF33"/>
                    </Style>
                </Border.Style>
                <TextBlock x:Name="TeksIkon" Text="✓" FontSize="16" FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <!-- Pesan -->
            <TextBlock x:Name="TeksPesan" Grid.Column="1"
                       FontSize="13" FontWeight="Medium" Foreground="White"
                       VerticalAlignment="Center" TextWrapping="Wrap"
                       LineHeight="19"/>
        </Grid>
    </Border>
</UserControl>
