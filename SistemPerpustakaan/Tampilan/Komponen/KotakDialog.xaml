<Window x:Class="SistemPerpustakaan.Tampilan.Komponen.KotakDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="" Height="200" Width="420"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize">
    <Border Background="White" CornerRadius="12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="0"
                              Opacity="0.18" Color="#000"/>
        </Border.Effect>
        <StackPanel Margin="28,24">
            <TextBlock x:Name="TeksIkon" FontSize="32"
                       HorizontalAlignment="Center" Margin="0,0,0,12"/>
            <TextBlock x:Name="TeksJudul" FontSize="16" FontWeight="Bold"
                       Foreground="#1E293B" TextAlignment="Center"/>
            <TextBlock x:Name="TeksPesan" FontSize="13" Foreground="#64748B"
                       TextAlignment="Center" TextWrapping="Wrap" Margin="0,8,0,24"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="TombolBatal" Content="Batal"
                        Style="{StaticResource TombolSekunder}"
                        Width="120" Height="40" Click="Batal_Click"/>
                <Button x:Name="TombolOk" Content="Ya, Lanjutkan"
                        Style="{StaticResource TombolUtama}"
                        Width="140" Height="40" Margin="12,0,0,0"
                        Click="Ok_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
